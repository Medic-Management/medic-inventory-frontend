// Environment para desarrollo local
export const environment = {
  production: false,
  // Para desarrollo local (descomentada por defecto)
  apiUrl: 'http://localhost:8080/api'

  // Para conectar al servidor desde local (comentar la de arriba y descomentar esta)
  // apiUrl: 'http://172.200.21.101:8080/api'
};
